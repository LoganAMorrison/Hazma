# Follow-ups

The durable backlog of ideas and deferred work that hasn't been promoted
to a `projects/<slug>/` plan yet.

Open items live in [`todo/`](todo/); resolved items move to
[`done/`](done/), so `ls todo/` is the live backlog at a glance. Items
move between the two directories; they are never deleted — the historical
reasoning is worth keeping.

The full lifecycle (create → resolve → repoint inbound links), when to
add one, and why this is not GitHub issues, live in
[`../workflow.md#follow-ups`](../workflow.md#follow-ups).

## Creating one

```sh
cp docs/followups/_template.md docs/followups/todo/<slug>.md
# fill in the fields, then add a row to the Open table below
```

## Open

| Item | Added | Source | Scope |
| --- | --- | --- | --- |
| [Citation checker skips deleted in-repo files](todo/citation-checker-skips-deleted-inrepo-files.md) | 2026-08-05 | PR #42 review | cross-cutting |
| [remaining `sqrt(kallen_lambda(...))` call sites](todo/kallen-under-sqrt-remaining-call-sites.md) | 2026-08-05 | carved out of the `cross_section_prefactor` fix | cross-cutting |
| [`kinematically_accessable` kept out of the public surface](todo/utils-public-surface-redundant-helpers.md) | 2026-08-05 | docs audit of `utils.rst` | cross-cutting |
| [model spectrum dicts reject scalar energies](todo/model-spectra-reject-scalar-energies.md) | 2026-08-08 | cython-to-rust Task 1.4 | cross-cutting |
| [the φ photon spectrum omits its direct `φ → π⁰γ` line](todo/phi-omits-its-direct-pi0-photon-line.md) | 2026-09-07 | parity-pinned-defect-repair Task 6 | cross-cutting |
| [rho photon outer boost can miss its support](todo/rho-photon-outer-boost-misses-support.md) | 2026-09-18 | parity-pinned-defect-repair Task 8 | cross-cutting |
| [four scalar elastic cross sections cancel away every significant bit](todo/scalar-elastic-cross-sections-cancel-in-atan-difference.md) | 2026-08-18 | closing the parity-corpus follow-up | cross-cutting |
| [the review-lessons ledger is past its working-set cap](todo/lessons-ledger-over-its-working-set-cap.md) | 2026-08-19 | PR #72 review round 1 | cross-cutting |
| [two vector cross sections raise `TypeError` at `e_cm = 2 m_x`](todo/vector-cross-sections-raise-at-the-two-mx-threshold.md) | 2026-08-20 | cython-to-rust Task 5.1 | cross-cutting |
| [mediator spectra return 0.0 for an unrecognised mode string](todo/mediator-spectra-accept-unknown-mode-strings.md) | 2026-08-23 | cython-to-rust Task 6.1 | cross-cutting |
| [`[mutation-harness-poisons-its-own-baseline]` is cited but not in the ledger](todo/lessons-ledger-missing-the-mutation-harness-class.md) | 2026-08-23 | cython-to-rust Task 6.2 | cross-cutting |
| [moved follow-ups leave dangling inbound paths](todo/moved-followups-leave-dangling-inbound-paths.md) | 2026-08-27 | PR #81 review | cross-cutting |
| [four tracked non-source files under `hazma/`](todo/tracked-non-source-files-under-hazma.md) | 2026-08-29 | cython-to-rust Task 7.3 | commit |
| [consolidate the divergent constants tables](todo/consolidate-the-two-constants-tables.md) | 2026-08-29 | cython-to-rust retrospective §5 | cross-cutting |
| [the pure-Python thermal averages return `0.0` for every `x >= 25`](todo/thermal-fallback-upper-limit-collapses-at-x-25.md) | 2026-09-06 | PR #91 review round 1 | cross-cutting |
| [free-threaded `abi3t` wheels](todo/free-threaded-abi3t-wheels.md) | 2026-08-29 | cython-to-rust retrospective §5 | cross-cutting |
| [the relic-density Boltzmann solve in Rust](todo/relic-density-odes-in-rust.md) | 2026-08-29 | cython-to-rust retrospective §5 | cross-cutting |
| [wheels for linux-aarch64 and Windows](todo/wheels-for-aarch64-and-windows.md) | 2026-08-29 | cython-to-rust retrospective §5 | commit |
| [`[tool.ruff]` uses the deprecated top-level keys](todo/ruff-config-uses-deprecated-top-level-keys.md) | 2026-09-06 | carved out of the isort/ruff gate repair | cross-cutting |
| [`scripts/` is outside every format and lint gate](todo/scripts-are-outside-the-format-and-lint-gates.md) | 2026-09-06 | surfaced by the isort/ruff gate repair | cross-cutting |
| [every decaying-theory positron channel evaluates the last channel](todo/decaying-theory-positron-channels-share-the-last-closure.md) | 2026-09-20 | PR #99 review | commit |
| [the INT_MAX `limit` test allocates 16 GiB inside scipy](todo/quad-limit-test-allocates-16-gib-in-scipy.md) | 2026-09-24 | parity-pinned-defect-repair Task 12 preflight | commit |
| [the mediator decay spectra lose their support at large boost](todo/mediator-decay-angular-windows-miss-their-support.md) | 2026-09-25 | the charged-pion neutrino continuum repair (`C2`) | cross-cutting |
| [the charged-pion positron clip turns a `NaN` energy into a spectrum](todo/positron-pion-clip-turns-nan-into-a-spectrum.md) | 2026-09-25 | PR #105 review | commit |

## Promoted / Done / Pruned

| Item | Status | Resolution |
| --- | --- | --- |
| [the charged pion's neutrino continuum loses its quadrature support](done/neutrino-pion-continuum-loses-its-quadrature-support.md) | done | Parity roster entry `C2`. `rust/src/kernels/neutrino_pion.rs` clips the boost window at the muon spectrum's endpoint, 69.78356271700862 MeV, so a boosted pion keeps its muon-decay continuum. Both rows of `dnde_neutrino_charged_pion` move: the continuum held 21% and 18% of its neutrinos at `E_π = 5` GeV and now holds all of them. 430 corpus values in the six arrays B5 declares, composed as `B5+C2`. The angular version of the same failure is [its own follow-up](todo/mediator-decay-angular-windows-miss-their-support.md). |
| [the mediator positron line is low by the electron's velocity](done/mediator-positron-line-misses-the-electron-velocity.md) | done | Parity roster entry `C1`, the first post-project label. `mediator_decay_positron::spectrum_point` divides the `e⁺e⁻` box by `r = sqrt(1 − 4 m_e²/m²)`, so it integrates to `pw_ee` rather than `pw_ee · r`. Every mediator positron spectrum rises inside the line window by `1/r − 1`, which is 8.4e-6 at 250 MeV, 1.7e-6 at 550 MeV and 6.4e-7 at 900 MeV relative to the line. That is 8,912 corpus positions in 192 arrays, 128 of them composed with A4 as `A4+C1`. `boost_delta_function` already carried the factor. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Recorded in `CHANGELOG.md`. |
| [the delta-declaration layer outlives the project that owns its ADR](done/delta-declaration-layer-outlives-its-project.md) | done | Resolved with its first trigger. The project's ADR-0001 is re-filed as repo-wide [ADR-0003](../adrs/ADR-0003-corpus-repairs-are-declared-deltas.md), which also carries the project rules that bind any repair. A post-project repair takes the next `C<n>` label in landing order, lists itself in `test/parity/README.md`, "Repairs", and cites its resolved follow-up as evidence. |
| [the boost integral mis-covers its window at both ends](done/boost-integral-drops-last-interior-cell.md) | done | [PR #93](https://github.com/LoganAMorrison/Hazma/pull/93) — `parity-pinned-defect-repair` Task 4, roster entry `A1`. `rust/src/boost.rs` covers `[x[ihigh-1], x[ihigh]]` and the clamped final row, and a window inside one cell is no longer covered twice. All seven tabulated photon spectra move, 4,154 of 10,045 pinned positions, and the sign splits: near threshold (`rest_plus_eps`) they fall from a median 9,768x too high, in flight they rise by a median 3.3% at `γ = 1.05` to 7.1e-5 at `γ = 10`. Reproduces the Task 2 Cython capture bit for bit. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the η′ two-photon line carries one photon instead of two](done/eta-prime-two-photon-line-missing-factor-two.md) | done | [PR #94](https://github.com/LoganAMorrison/Hazma/pull/94) — `parity-pinned-defect-repair` Task 5, roster entry `B1`. `ETAP_TO_A_A_WEIGHT` is `2 BR(η′ → γγ)`; the yield rises by exactly `BR = 0.02307` photons per decay at any boost, 189 corpus positions, declared as the composite `A1+B1`. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the φ photon lines sit at the daughter meson's energy](done/phi-photon-lines-use-the-daughter-meson-energy.md) | done | [PR #95](https://github.com/LoganAMorrison/Hazma/pull/95) — `parity-pinned-defect-repair` Task 6, roster entry `B2`. Both lines sit at `(M_φ² − m²)/(2 M_φ)`: 656.942 → 362.519 MeV and 959.646 → 59.815 MeV. The yield does not move (0.013092 per decay, relocated), so the gate is a position assertion; 305 corpus positions, declared as `A1+B2`. The missing `φ → π⁰γ` line it asked to be checked is [its own follow-up](todo/phi-omits-its-direct-pi0-photon-line.md). The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the muon photon spectrum's rest frame stops short of the endpoint](done/photon-muon-rest-frame-endpoint-uses-the-wrong-power-of-r.md) | done | [PR #96](https://github.com/LoganAMorrison/Hazma/pull/96) — `parity-pinned-defect-repair` Task 7, roster entry `A2`. The rest-frame guard is `y ≥ 1 − r`; 0.254 MeV of support is restored, a net 5.445e-8 photons per decay. The signed O(α) tail is retained under that project's ADR-0002. Four corpus positions move, all in `spectra.photon.muon[rest]`. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the charged-pion photon spectrum returns zero in the forward cone](done/charged-pion-photon-spectrum-misses-the-forward-cone.md) | done | [PR #97](https://github.com/LoganAMorrison/Hazma/pull/97) — `parity-pinned-defect-repair` Task 8, roster entry `A3`. The angular integral follows the physical support: `dnde_photon_charged_pion(900, 1396)` goes from 0 to 3.585860e-7 MeV⁻¹, and 6,359 corpus values move across the pion, both rhos and three mediator entry points. The rho's *outer* support failure stays open as [its own follow-up](todo/rho-photon-outer-boost-misses-support.md). The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [both rho photon spectra return the boost integrand at rest](done/rho-rest-frame-branch-returns-the-integrand.md) | done | [PR #98](https://github.com/LoganAMorrison/Hazma/pull/98) — `parity-pinned-defect-repair` Task 9, roster entry `B3`. At `E_ρ = m_ρ` both rho spectra return the rest-frame spectrum rather than the boost integrand, so each nonzero rest value is multiplied by `E_γ` in MeV; 350 corpus values, composed with the A3 capture as `A3+B3`. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the muon positron spectrum divides by its normalization](done/positron-muon-spectrum-normalization-inverted.md) | done | [PR #99](https://github.com/LoganAMorrison/Hazma/pull/99) — `parity-pinned-defect-repair` Task 10, roster entry `A4`. `dnde_positron_muon` multiplies by its normalization; every positron spectrum built on the muon rises by up to `N² = 1.000374206647938`, and the Michel spectrum integrates to 1 (shipped 0.999626). 21,975 corpus values across six cases, bit for bit against the Task 2 capture for the muon. The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [the charged pion's `pi -> e nu` neutrino line is added twice](done/neutrino-pion-electron-line-counted-twice.md) | done | [PR #90](https://github.com/LoganAMorrison/Hazma/pull/90) — `parity-pinned-defect-repair` Task 10a, roster entry `B5`. The `π → e ν_e` line comes from one half only; the electron-neutrino row loses `BR = 1.230e-4` per pion (0.0123% integrated, exactly ×0.5 above the continuum's support) and the muon row is bit-identical. 215 corpus values. The hard zeros it exposed are [their own follow-up](done/neutrino-pion-continuum-loses-its-quadrature-support.md). The corpus keeps its pre-repair arrays; the moved positions are declared in `test/parity/deltas.py`. Moved here at the project's close (Task 12). |
| [`preflight.sh` isort/ruff gates are red on the trunk](done/preflight-isort-ruff-red-on-trunk.md) | done | [PR #89](https://github.com/LoganAMorrison/Hazma/pull/89) — option 2 (narrow the gate to the diff). `scripts/agents/lint_delta.py` runs each linter over the working tree and over the same paths at the merge base, and gates 2 and 3 report only the difference — `0 new (6091 pre-existing at <sha>)` rather than a FAIL every branch inherits. Findings are matched on `(path, rule, message)` with position dropped, so an edit near the top of a file does not resurface everything below it; `isort` is matched per file. The file's fourth ask needed no `--no-python` flag: a diff touching no Python leaves the two trees identical, so the rows pass on their own in 0.4 s. The 6091 ruff and 72 isort findings are untouched and still real — option 1 stays the durable end state, now payable file by file instead of per PR, since each drop is credited as `N fixed`. |
| [`thermal_cross_section` returns its integrator's initial estimate](done/thermal-cross-section-quadrature-never-converges.md) | done | `parity-pinned-defect-repair` Task 13, as roster entry `B6`. `epsabs = 0` at all four call sites — both Rust kernels plus the two pure-Python ones the port never touched — and `THERMAL_LIMIT` raised from 50 to 100, without which the criterion could not be reached at 33 of the 540 corpus positions. **The file's own 0.5%–5% estimate was measured at one model point and understated the defect**: swept over all 570 pinned corpus positions against scipy's QUADPACK at `epsrel = 1e-12`, the shipped ⟨σv⟩ is wrong by up to 100%, and `relic_density` moves by −99.9% to +2.4% over the six pinned model points. The corpus keeps its pre-repair arrays; the 539 moved positions are declared through a new `Reference` relation in `test/parity/deltas.py`, since the stored value is the true one unresolved rather than shifted by a term the physics names. Recorded in `CHANGELOG.md`. |
| [markdownlint was never run over `.claude/skills/`](done/markdownlint-skips-skill-file-shapes.md) | done | [PR #88](https://github.com/LoganAMorrison/Hazma/pull/88) — resolved by option 2 (normalize the docs), not the option 1 the file recommended: its premise that every `SKILL.md` opens with a bolded label was false, six of eight already used real headings. The four labels in `review-plan` and `task-pipeline` are now `##`, and five `MD031`/`MD032` errors inside a blockquoted subagent prompt took quoted blank lines rather than bare ones, so the 79-line blockquote stayed contiguous. Both skill trees now lint clean, and `preflight.md` says they are in the gate's scope. |
| [the scalar mediator decay spectrum radiated half its FSR photons](done/scalar-decay-fsr-half-normalized.md) | done | [PR #87](https://github.com/LoganAMorrison/Hazma/pull/87) — filed and repaired together. `rust/src/kernels/scalar_decay_photon.rs` doubles both rest-frame FSR coefficients (`PAIR_NORMALIZATION`), pinned against the annihilation-side closed forms and the Altarelli-Parisi pair limit. The corpus keeps its pre-repair arrays; `test/parity/deltas.py`, the declared-delta layer `projects/parity-pinned-defect-repair` Task 1 specified, declares the 30 moved arrays as roster entry B4. `ScalarMediator.dnde_ss` photon yield rises 1.7–2.9% with the corpus couplings, pointwise up to 2× where FSR is the only open channel; recorded in `CHANGELOG.md`. |
| [`pip install -e .` builds `hazma._core` unoptimized](done/editable-installs-build-the-rust-extension-in-debug.md) | done | cython-to-rust Task 7.1 — resolved by the backend swap rather than by a choice. The debug default was `setuptools_rust.build_rust`'s `debug = self.inplace or self.debug`; maturin's PEP 517 hooks build release unconditionally, and only the `maturin develop` CLI (which this repo never invokes) defaults to debug. Measured after the cutover: `uv pip install -e .` leaves `rust/target/release/` and no `debug/`, and `thermal_cross_section(x=0.5)` runs at 35.8 us from the editable tree against the file's 1866 us debug figure. The switch also corrected this file's "the two profiles are numerically identical" risk note, which held for the functions Task 5.3 measured and not in general: under release the mediator table grids sit one ulp from `numpy.logspace` at 5 of 1000 sampled abscissae. No published value moved -- wheels have always been release builds -- but `test/test_core_mediator_tables.py`'s grid comparison had encoded the debug values as exact, and moved to its own derived one-ulp budget. |
| [sdist ships cythonized `*.c`, `docs/`, `test/`](done/sdist-ships-generated-c-and-docs.md) | done | cython-to-rust Task 7.1 — folded into the maturin cutover, the window this file named. `MANIFEST.in` is gone; `[tool.maturin]` answers all four items. The `*.c` question is moot (no transpiler since Task 6.4), `docs/`/`test/`/`notebooks/` are dropped, the sdist is built from the git index so working-directory state cannot reach it, and the `*.pyd` typo went with the whole `[tool.setuptools.package-data]` block. Measured on a clean tree: **415 files to 264**. Verified by source-installing the sdist into a fresh CPython 3.10 venv and import-smoking from outside the repo. |
| [the oracle roster has no restore revision for the deleted mediator spectrum `.pyx`](done/oracle-restore-revisions-for-the-mediator-decay-pyx.md) | done | cython-to-rust Task 6.4 — `defects.RESTORED_SOURCES` goes from 13 entries to 29 and every case is now covered. The four mediator modules resolve to `7594761^` (Task 6.2) and `c384aff^` (Task 6.3), both merged by then. The twelve files Task 6.4 itself deleted could not use that spelling for the reason 6.2 and 6.3 could not — a task cannot know its own commit — so they are pinned to a revision that already existed, `1b022d4`, where all of them are present in final form; `git show <rev>:<path>` does not care which spelling it is given. Verified by resolving all 29 against git. The roster now also lists the headers and cimported twins a restore has to compile against, and `oracles/README.md` says that a re-capture must restore `setup.py` and `pyproject.toml` too. |
| [positron spectra return `nan` at the legacy `MASS_E`](done/positron-spectrum-nan-at-legacy-electron-mass.md) | done | cython-to-rust Task 6.3 — the `nan` was a clang FMA contraction of `sqrt(eng_p*eng_p - me*me)`, whose radicand is the upward rounding of `me*me` (1.45e-17) and so negative at `eng_p == m_e`, not a consequence of the two `MASS_E` tables diverging. Resolved by the file's second option: `mediator_decay_positron.rs`'s `momentum` keeps the fused spelling and clamps the radicand at zero, moving that one double from `nan` to `0.0` and no other value. Consolidating the tables stays open under `rules.md` rule 4. Pinned in `test/test_core_mediator_positron.py::TestTheThresholdSingularity` rather than in the corpus, which rule 2 forbids. |
| [parity corpus pins ill-conditioned points](done/parity-corpus-pins-ill-conditioned-points.md) | done | `test/parity/stability.py` masks the 494 stored positions whose values are cancellation residue, established against `test/parity/reference.py` (the same closed forms at 60 digits) rather than against platform disagreement; `tolerances.PLATFORM_EXACT_RTOL` and `PLATFORM_SPECFUN_RTOL` give those two classes an off-libm budget and `tolerances.zero_floor` handles the four declared stored zeros a change of libm moves. CI's `--ignore=test/parity` scoping removed; `pytest test/parity` is **637 passed, 1 skipped** on macOS/arm64, Linux/aarch64 and Linux/x86_64. The underlying kernel defect is carved out to [its own follow-up](todo/scalar-elastic-cross-sections-cancel-in-atan-difference.md). |
| [`test_core_interp.py` scoped its NumPy oracle with a probe](done/interp-oracle-scoped-by-an-unsound-probe.md) | done | Probe replaced by `ON_THE_CAPTURING_PLATFORM` + a measured, peak-scaled `OFF_PLATFORM_BUDGET = 1e-12`; `TestFusedArithmetic` rewritten against a full Python transcription of `rust/src/interp.rs`. Module goes from `24 passed, 9 skipped` to **`42 passed, 0 skipped`** on linux/amd64. |
| [markdownlint config for templates](done/markdownlint-config-for-templates.md) | done | Committed [`.markdownlint.jsonc`](../../.markdownlint.jsonc) encoding the repo's shapes (frontmatter-title phase files, `<placeholder>` notation, wide fact tables); all 18 inline pragmas removed. `docs/` + `projects/` errors 132 → 0. |
| [`black` pin diverges between pyproject and CI](done/black-pin-divergence-pyproject-vs-ci.md) | done | [PR #40](https://github.com/LoganAMorrison/Hazma/pull/40) — pins moved to a single PEP 735 `lint` dependency group that CI installs; repo reformatted with black 26.x (33 files). |
| [`msqrd`-driven Monte-Carlo FSR generator](done/msqrd-driven-fsr-generator.md) | done | `hazma.spectra.dnde_photon_fsr` (ADR-0001, [PR #41](https://github.com/LoganAMorrison/Hazma/pull/41)) |
| [`cross_section_prefactor` threshold cancellation](done/cross-section-prefactor-threshold-cancellation.md) | done | `hazma.utils.two_body_momentum` — factored Källén, heavier mass first; ≤4.4e-16 relative to threshold. Remaining λ-under-sqrt sites carved out to [their own follow-up](todo/kallen-under-sqrt-remaining-call-sites.md). |
| [`WIDTH_K`/`WIDTH_PI` exponent bug](done/legacy-parameters-width-exponent-bug.md) | done | Both names deleted from `hazma/_utils/legacy_parameters.pxd` (no consumer; `constants.pxd` is canonical). No published value moves. |
