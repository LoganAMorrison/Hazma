import numpy as np
import numpy.typing as npt


def generate_point(masses: npt.ArrayLike, cme: float, num_fsp: int):
    ...


def generate_space(num_ps_pts: int, masses: npt.ArrayLike, cme: float, num_fsp: int):
    ...
